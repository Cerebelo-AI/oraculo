diff -ruN a/keyboard.c b/keyboard.c
--- wvkbd-up/keyboard.c	2026-09-23 20:39:01
+++ b/keyboard.c
@@ -1,5 +1,6 @@
 #include "proto/virtual-keyboard-unstable-v1-client-protocol.h"
 #include <linux/input-event-codes.h>
+#include <signal.h>
 #include <stddef.h>
 #include <stdio.h>
 #include <sys/mman.h>
@@ -291,6 +292,8 @@
         zwp_virtual_keyboard_v1_key(vkbd, time, KEY_RIGHTALT, state);
 }
 
+#define DEFERRED_MODS (Ctrl | Alt | Super)
+
 void
 kbd_unpress_key(struct kbd *kb, uint32_t time)
 {
@@ -428,6 +431,8 @@
 
     switch (k->type) {
     case Code:
+        if (kb->mods & DEFERRED_MODS)
+            zwp_virtual_keyboard_v1_key_mods(kb->vkbd, time, kb->mods & DEFERRED_MODS, WL_KEYBOARD_KEY_STATE_PRESSED);
         if (k->code_mod) {
             zwp_virtual_keyboard_v1_key_mods(kb->vkbd, time, k->code_mod, WL_KEYBOARD_KEY_STATE_PRESSED);
             if (k->reset_mod) {
@@ -462,8 +467,15 @@
         break;
     case Mod:
         kb->mods ^= k->code;
-        if (kb->mods & k->code) {
+        /* Deck (Fable, 2026-09-22): Ctrl/Alt/Super are latched locally and only
+         * sent to the compositor together with the next key. Announcing Super
+         * immediately made Hyprland treat the following touch as Super+click
+         * (window grab) and the key never reached the keyboard. */
+        if (k->code & DEFERRED_MODS) {
             if (kb->debug)
+                fprintf(stderr, "mod latched locally (deferred)\n");
+        } else if (kb->mods & k->code) {
+            if (kb->debug)
                 fprintf(stderr, "mod pressed\n");
             zwp_virtual_keyboard_v1_key_mods(kb->vkbd, time, k->code, WL_KEYBOARD_KEY_STATE_PRESSED);
         } else {
@@ -480,8 +492,11 @@
                 kbd_draw_key(kb, k, Unpress);
             }
         }
-        zwp_virtual_keyboard_v1_modifiers(kb->vkbd, kb->mods, 0, 0, 0);
+        zwp_virtual_keyboard_v1_modifiers(kb->vkbd, kb->mods & ~DEFERRED_MODS, 0, 0, 0);
         break;
+    case Hide:
+        raise(SIGUSR1);
+        break;
     case Layout:
         // switch to the layout determined by the key
         kbd_switch_layout(kb, k->layout, kbd_get_layer_index(kb, k->layout));
@@ -528,6 +543,8 @@
         break;
     case Copy:
         // copy code as unicode chr by setting a temporary keymap
+        if (kb->mods & DEFERRED_MODS)
+            zwp_virtual_keyboard_v1_key_mods(kb->vkbd, time, kb->mods & DEFERRED_MODS, WL_KEYBOARD_KEY_STATE_PRESSED);
         kb->last_swipe = kb->last_press = k;
         kbd_draw_key(kb, k, Press);
         if (kb->mods & Shift) {
diff -ruN a/keyboard.h b/keyboard.h
--- wvkbd-up/keyboard.h	2026-09-23 20:39:01
+++ b/keyboard.h
@@ -24,6 +24,7 @@
 	NextLayer, // Layout switch to the next layout in the layers sequence
 	Compose,   // Compose modifier key, switches to a specific associated layout
 	           // upon next keypress
+	Hide,      // Collapse the keyboard (same as SIGUSR1)
 	EndRow,    // Incidates the end of a key row
 	Last,      // Indicated the end of a layout
 };
diff -ruN a/layout.deskintl.h b/layout.deskintl.h
--- wvkbd-up/layout.deskintl.h	2026-09-23 20:39:01
+++ b/layout.deskintl.h
@@ -11,6 +11,7 @@
 /* layout declarations */
 enum layout_id {
 	Full = 0,
+	Deck,
 	Special,
 	Cyrillic,
 	ComposeA,
@@ -55,7 +56,7 @@
 	NumLayouts,
 };
 
-static struct key keys_full[], keys_special[], keys_cyrillic[],
+static struct key keys_full[], keys_deck[], keys_special[], keys_cyrillic[],
   keys_compose_a[],
   keys_compose_e[], keys_compose_y[], keys_compose_u[], keys_compose_i[],
   keys_compose_o[], keys_compose_w[], keys_compose_r[], keys_compose_t[],
@@ -70,7 +71,8 @@
   keys_compose_cyr_k[], keys_index[];
 
 static struct layout layouts[NumLayouts] = {
-  [Full] = {keys_full, "latin", "full", true}, // second parameter is the keymap name
+  [Full] = {keys_full, "latin", "full", true},
+  [Deck] = {keys_deck, "latin", "deck", true}, // second parameter is the keymap name
                                          // third parameter is the layout name
 										 // last parameter indicates if it's an alphabetical/primary layout
   [Special] = {keys_special, "latin", "special", false},
@@ -239,12 +241,103 @@
   {"", "", 0.0, Last},
 };
 
+/* Steam Deck: 5 rows, no F-row (F-keys live in the special layer), with a
+ * collapse key (Hide) in the bottom-right corner. Width = 14.5 like `full`. */
+static struct key keys_deck[] = {
+  {"Esc", "Esc", 1.0, Code, KEY_ESC, .scheme = 1},
+  {"1", "!", 1.0, Code, KEY_1},
+  {"2", "@", 1.0, Code, KEY_2},
+  {"3", "#", 1.0, Code, KEY_3},
+  {"4", "$", 1.0, Code, KEY_4},
+  {"5", "%", 1.0, Code, KEY_5},
+  {"6", "^", 1.0, Code, KEY_6},
+  {"7", "&", 1.0, Code, KEY_7},
+  {"8", "*", 1.0, Code, KEY_8},
+  {"9", "(", 1.0, Code, KEY_9, &layouts[ComposeBracket]},
+  {"0", ")", 1.0, Code, KEY_0, &layouts[ComposeBracket]},
+  {"-", "_", 1.0, Code, KEY_MINUS, &layouts[ComposeBracket]},
+  {"=", "+", 1.0, Code, KEY_EQUAL, &layouts[ComposeBracket]},
+  {"⌫", "⌫", 1.5, Code, KEY_BACKSPACE, .scheme = 1},
+  {"", "", 0.0, EndRow},
+
+  {"Tab", "Tab", 1.5, Code, KEY_TAB, .scheme = 1},
+  {"q", "Q", 1.0, Code, KEY_Q},
+  {"w", "W", 1.0, Code, KEY_W, &layouts[ComposeW]},
+  {"e", "E", 1.0, Code, KEY_E, &layouts[ComposeE]},
+  {"r", "R", 1.0, Code, KEY_R, &layouts[ComposeR]},
+  {"t", "T", 1.0, Code, KEY_T, &layouts[ComposeT]},
+  {"y", "Y", 1.0, Code, KEY_Y, &layouts[ComposeY]},
+  {"u", "U", 1.0, Code, KEY_U, &layouts[ComposeU]},
+  {"i", "I", 1.0, Code, KEY_I, &layouts[ComposeI]},
+  {"o", "O", 1.0, Code, KEY_O, &layouts[ComposeO]},
+  {"p", "P", 1.0, Code, KEY_P, &layouts[ComposeP]},
+  {"[", "{", 1.0, Code, KEY_LEFTBRACE},
+  {"]", "}", 1.0, Code, KEY_RIGHTBRACE},
+  {"\\", "|", 1.0, Code, KEY_BACKSLASH},
+  {"", "", 0.0, EndRow},
+
+  {"Cmp", "Cmp", 1.0, Compose, .scheme = 1},
+  {"a", "A", 1.0, Code, KEY_A, &layouts[ComposeA]},
+  {"s", "S", 1.0, Code, KEY_S, &layouts[ComposeS]},
+  {"d", "D", 1.0, Code, KEY_D, &layouts[ComposeD]},
+  {"f", "F", 1.0, Code, KEY_F, &layouts[ComposeF]},
+  {"g", "G", 1.0, Code, KEY_G, &layouts[ComposeG]},
+  {"h", "H", 1.0, Code, KEY_H, &layouts[ComposeH]},
+  {"j", "J", 1.0, Code, KEY_J, &layouts[ComposeJ]},
+  {"k", "K", 1.0, Code, KEY_K, &layouts[ComposeK]},
+  {"l", "L", 1.0, Code, KEY_L, &layouts[ComposeL]},
+  {";", ":", 1.0, Code, KEY_SEMICOLON},
+  {"'", "\"", 1.0, Code, KEY_APOSTROPHE, &layouts[ComposeBracket]},
+  {"⏎", "⏎", 2.5, Code, KEY_ENTER, .scheme = 1},
+  {"", "", 0.0, EndRow},
+
+  {"⇧", "⇫", 2.0, Mod, Shift, .scheme = 1},
+  {"z", "Z", 1.0, Code, KEY_Z, &layouts[ComposeZ]},
+  {"x", "X", 1.0, Code, KEY_X, &layouts[ComposeX]},
+  {"c", "C", 1.0, Code, KEY_C, &layouts[ComposeC]},
+  {"v", "V", 1.0, Code, KEY_V, &layouts[ComposeV]},
+  {"b", "B", 1.0, Code, KEY_B, &layouts[ComposeB]},
+  {"n", "N", 1.0, Code, KEY_N, &layouts[ComposeN]},
+  {"m", "M", 1.0, Code, KEY_M, &layouts[ComposeM]},
+  {",", "<", 1.0, Code, KEY_COMMA, &layouts[ComposeMath]},
+  {".", ">", 1.0, Code, KEY_DOT, &layouts[ComposePunctuation]},
+  {"/", "?", 1.0, Code, KEY_SLASH},
+  {"↑", "↑", 1.0, Code, KEY_UP, .scheme = 1},
+  {"Del", "Del", 1.5, Code, KEY_DELETE, .scheme = 1},
+  {"", "", 0.0, EndRow},
+
+  {"⌨͕", "⌨͔", 1.0, NextLayer, .scheme = 1},
+  {"Ctr", "Ctr", 1.0, Mod, Ctrl, .scheme = 1},
+  {"Sup", "Sup", 1.0, Mod, Super, .scheme = 1},
+  {"Alt", "Alt", 1.0, Mod, Alt, .scheme = 1},
+  {"", "", 5.0, Code, KEY_SPACE},
+  {"AGr", "AGr", 1.0, Mod, AltGr, .scheme = 1},
+  {"←", "←", 1.0, Code, KEY_LEFT, .scheme = 1},
+  {"↓", "↓", 1.0, Code, KEY_DOWN, .scheme = 1},
+  {"→", "→", 1.0, Code, KEY_RIGHT, .scheme = 1},
+  {"⌄", "⌄", 1.5, Hide, .scheme = 1},
+  /* end of layout */
+  {"", "", 0.0, Last},
+};
+
 static struct key keys_special[] = {
   {"", "", 13.25, Pad},
   {"Ins", "Ins", 1.25, Code, KEY_INSERT, .scheme = 1},
   {"", "", 0.0, EndRow},
 
-  {"", "", 14.5, Pad},
+  {"F1", "F1", 1.0, Code, KEY_F1, .scheme = 1},
+  {"F2", "F2", 1.0, Code, KEY_F2, .scheme = 1},
+  {"F3", "F3", 1.0, Code, KEY_F3, .scheme = 1},
+  {"F4", "F4", 1.0, Code, KEY_F4, .scheme = 1},
+  {"F5", "F5", 1.0, Code, KEY_F5, .scheme = 1},
+  {"F6", "F6", 1.0, Code, KEY_F6, .scheme = 1},
+  {"F7", "F7", 1.0, Code, KEY_F7, .scheme = 1},
+  {"F8", "F8", 1.0, Code, KEY_F8, .scheme = 1},
+  {"F9", "F9", 1.0, Code, KEY_F9, .scheme = 1},
+  {"F10", "F10", 1.0, Code, KEY_F10, .scheme = 1},
+  {"F11", "F11", 1.0, Code, KEY_F11, .scheme = 1},
+  {"F12", "F12", 1.0, Code, KEY_F12, .scheme = 1},
+  {"", "", 2.5, Pad},
   {"", "", 0.0, EndRow},
 
   {"", "", 14.5, Pad},
@@ -267,7 +360,7 @@
   {"Alt", "Alt", 1.0, Mod, Alt, .scheme = 1},
   {"", "", 5.0, Pad},
   {"AGr", "AGr", 1.0, Mod, AltGr, .scheme = 1},
-  {"Ctr", "Ctr", 1.0, Mod, Ctrl, .scheme = 1},
+  {"⌄", "⌄", 1.0, Hide, .scheme = 1},
   {"Home", "Home", 1.0, Code, KEY_HOME, .scheme = 1},
   {"PgDn", "PgDn", 1.0, Code, KEY_PAGEDOWN, .scheme = 1},
   {"End", "End", 1.0, Code, KEY_END, .scheme = 1},
